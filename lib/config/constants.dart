class AppConstants {
  // App info
  static const String appName = 'Lecture Tracker';
  static const String appVersion = '1.0.0';

  // API endpoints
  static const String baseUrl = '';

  // Timeouts
  static const Duration requestTimeout = Duration(seconds: 30);

  // Pagination
  static const int pageSize = 20;
}
