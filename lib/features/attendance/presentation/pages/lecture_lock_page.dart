import 'package:flutter/material.dart';

class LectureLockPage extends StatelessWidget {
  const LectureLockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lecture Lock')),
      body: const Center(child: Text('Lecture Lock Page')),
    );
  }
}
