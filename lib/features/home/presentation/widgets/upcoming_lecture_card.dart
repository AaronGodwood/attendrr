import 'package:flutter/material.dart';

class UpcomingLectureCard extends StatelessWidget {
  const UpcomingLectureCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Placeholder());
  }
}
