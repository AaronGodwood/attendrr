import 'package:flutter/material.dart';

class LinkTimetablePage extends StatelessWidget {
  const LinkTimetablePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Link Timetable')),
      body: const Center(child: Text('Link Timetable Page')),
    );
  }
}
